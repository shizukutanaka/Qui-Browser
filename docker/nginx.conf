# Nginx Configuration for Qui Browser VR
# Optimized for WebXR VR applications with enhanced security

server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    # ルートディレクトリ
    root /usr/share/nginx/html;
    index index.html;

    # ログ設定
    access_log /var/log/nginx/access.log combined;
    error_log /var/log/nginx/error.log warn;

    # セキュリティヘッダー（エンタープライズレベル）
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "0" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; connect-src 'self' https: http://localhost:* http://127.0.0.1:* http://[::1]:*; img-src 'self' data: https:; font-src 'self' data:; media-src 'self' https:; frame-src 'none'; worker-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self';" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), magnetometer=(), gyroscope=(), accelerometer=(), ambient-light-sensor=(), autoplay=(), encrypted-media=(), fullscreen=(self), payment=()" always;

    # Gzip圧縮
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss
               application/atom+xml image/svg+xml;
    gzip_min_length 1000;
    gzip_disable "msie6";

    # Brotli圧縮（より効率的）
    brotli on;
    brotli_comp_level 6;
    brotli_types text/plain text/css text/xml text/javascript
                 application/json application/javascript application/xml+rss
                 application/atom+xml image/svg+xml;

    # Service Worker用の設定 — sw.js は削除済み、実ファイル名は service-worker.js
    # （JS の汎用 immutable キャッシュルールに捕まると SW 更新が伝播しない）
    location = /service-worker.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
        add_header Service-Worker-Allowed "/";

        # CORS（必要に応じて）
        add_header Access-Control-Allow-Origin "*";

        types {
            application/javascript js;
        }
    }

    # offline.js はハッシュなしの mutable ファイル — 汎用 js immutable に
    # 捕まえると更新が永遠に伝播しない（SW と同じ問題クラス、exact match で除外）
    location = /offline.js {
        expires 1h;
        add_header Cache-Control "public, max-age=3600";
        add_header X-Content-Type-Options "nosniff";
        types {
            application/javascript js;
        }
    }

    # JavaScript/CSSのキャッシュ設定（長期キャッシュ — js/ と assets/ は
    # コンテンツハッシュ付きのみ到達、ルート直下の .js は個別ルールが先に当たる）
    location ~* \.(js|css)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        add_header X-Content-Type-Options "nosniff";

        types {
            application/javascript js;
            text/css css;
        }
    }

    # 画像/アイコンのキャッシュ設定
    location ~* \.(png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        add_header X-Content-Type-Options "nosniff";
    }

    # manifest.json
    location = /manifest.json {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
        add_header Content-Type "application/manifest+json; charset=utf-8";
    }

    # HTMLファイル（短期キャッシュ）
    # try_files の内部リダイレクトはこの location に再マッチするため、
    # location / 側の Permissions-Policy は HTML 応答に届かない — ここで明示。
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, max-age=3600";
        add_header X-Frame-Options "DENY";
        add_header X-Content-Type-Options "nosniff";
        add_header X-XSS-Protection "0";
        add_header Referrer-Policy "strict-origin-when-cross-origin";
        add_header Permissions-Policy "accelerometer=*, camera=*, gyroscope=*, magnetometer=*, microphone=*, xr-spatial-tracking=*" always;
        # This block defines its own add_headers, so it inherits NONE of the
        # server-level ones — the CSP must be restated or direct /index.html
        # requests ship without one.
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; connect-src 'self' https: http://localhost:* http://127.0.0.1:* http://[::1]:*; img-src 'self' data: https:; font-src 'self' data:; media-src 'self' https:; frame-src 'none'; worker-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self';" always;
    }

    # メインロケーション
    location / {
        try_files $uri $uri/ /index.html;

        # セキュリティヘッダー
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "0" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # WebXR用のPermissions Policy
        add_header Permissions-Policy "accelerometer=*, camera=*, gyroscope=*, magnetometer=*, microphone=*, xr-spatial-tracking=*" always;

        # Content Security Policy — keep in lockstep with the meta CSP in
        # index.html: the two AND together, so any difference is a silently
        # dead feature on this deploy target.
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; connect-src 'self' https: http://localhost:* http://127.0.0.1:* http://[::1]:*; img-src 'self' data: https:; font-src 'self' data:; media-src 'self' https:; frame-src 'none'; worker-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self';" always;
    }

    # アセットディレクトリ（Vite のハッシュ付きバンドル + バンドル済み画像）
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        add_header Access-Control-Allow-Origin "*";
    }

    # ヘルスチェックエンドポイント
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Nginxステータス（監視用、プロダクションでは無効化推奨）
    location /nginx_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }

    # 隠しファイルへのアクセス拒否
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # バックアップファイルへのアクセス拒否
    location ~ ~$ {
        deny all;
        access_log off;
        log_not_found off;
    }

    # よくある攻撃パターンのブロック
    location ~* (wp-admin|wp-login|xmlrpc\.php|\.git|\.svn|\.env) {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 404エラーページ
    error_page 404 /index.html;

    # 50xエラーページ
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# HTTPS設定（SSL証明書がある場合）
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name localhost;
#
#     # SSL証明書
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#
#     # SSL設定
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 上記のserver設定と同じ内容
#     # ...
# }

# HTTP -> HTTPS リダイレクト（HTTPS有効時）
# server {
#     listen 80;
#     listen [::]:80;
#     server_name localhost;
#     return 301 https://$server_name$request_uri;
# }
