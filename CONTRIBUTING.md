# Contributing to Qui Browser VR

まず、Qui Browser VRへの貢献を検討していただきありがとうございます！

このドキュメントは、プロジェクトへの貢献方法をガイドします。

## 目次

1. [行動規範](#行動規範)
2. [始める前に](#始める前に)
3. [開発環境のセットアップ](#開発環境のセットアップ)
4. [貢献の種類](#貢献の種類)
5. [プルリクエストのプロセス](#プルリクエストのプロセス)
6. [コーディング規約](#コーディング規約)
7. [コミットメッセージ](#コミットメッセージ)
8. [テスト](#テスト)
9. [ドキュメント](#ドキュメント)

---

## 行動規範

このプロジェクトは[Code of Conduct](CODE_OF_CONDUCT.md)を採用しています。参加することで、この規範を遵守することに同意したものとみなされます。

---

## 始める前に

### Issue の確認

- 既存の[Issues](https://github.com/yourusername/qui-browser-vr/issues)を確認して、同じ問題や提案がないか確認してください
- 大きな変更の場合は、事前にIssueを作成して議論することを推奨します

### 良い最初のIssue

初めての貢献者向けに、以下のラベルが付いたIssueを確認してください：

- `good first issue` - 初心者向けの簡単なタスク
- `help wanted` - サポートが必要なタスク
- `documentation` - ドキュメントの改善

---

## 開発環境のセットアップ

### 前提条件

- Node.js 14.0.0以上
- Git
- VRデバイス（Meta Quest、Picoなど）推奨
- HTTPS対応のローカルサーバー

### セットアップ手順

1. **リポジトリのフォーク**
   ```bash
   # GitHubでフォークボタンをクリック
   ```

2. **クローン**
   ```bash
   git clone https://github.com/[your-username]/qui-browser-vr.git
   cd qui-browser-vr
   ```

3. **依存関係のインストール**
   ```bash
   npm install
   ```

4. **ローカルサーバーの起動**
   ```bash
   npm run serve
   # または
   python -m http.server 8000
   ```

5. **VRデバイスでアクセス**
   ```
   http://[your-ip]:8000
   ```

6. **ブランチの作成**
   ```bash
   git checkout -b feature/your-feature-name
   # または
   git checkout -b fix/your-bug-fix
   ```

---

## 貢献の種類

### 1. バグ報告

バグを発見した場合は、以下の情報を含めてIssueを作成してください：

```markdown
## バグの説明
バグの簡潔な説明

## 再現手順
1. 〜を開く
2. 〜をクリックする
3. 〜が発生する

## 期待される動作
本来どうあるべきか

## 実際の動作
実際に何が起こったか

## 環境
- デバイス: Meta Quest 3
- ブラウザ: Meta Quest Browser 28.0
- VR Browser Version: 2.0.0
- OS: Android 12

## スクリーンショット
可能であれば添付
```

### 2. 機能提案

新しい機能を提案する場合：

```markdown
## 機能の説明
提案する機能の概要

## 動機
なぜこの機能が必要か

## 詳細設計
どのように実装するか（オプション）

## 代替案
検討した他の方法（オプション）

## その他
追加情報
```

### 3. コード貢献

- バグ修正
- 新機能の追加
- パフォーマンス改善
- リファクタリング

### 4. ドキュメント貢献

- READMEの改善
- APIドキュメントの追加
- 使用例の追加
- 翻訳

### 5. テスト貢献

- ユニットテストの追加
- E2Eテストの追加
- テストカバレッジの向上

---

## プルリクエストのプロセス

### 1. 変更の実装

```bash
# feature/fix ブランチで作業
git checkout -b feature/add-new-vr-feature

# ファイルを編集
# ...

# 変更をステージング
git add .

# コミット
git commit -m "feat: add new VR feature"
```

### 2. テストの実行

```bash
# ユニットテスト
npm test

# カバレッジ
npm run test:coverage
```

### 3. プッシュ

```bash
git push origin feature/add-new-vr-feature
```

### 4. プルリクエストの作成

GitHub上でプルリクエストを作成し、以下を記載：

```markdown
## 変更内容
このPRで何を変更したか

## 関連Issue
Closes #123

## 変更の種類
- [ ] バグ修正
- [x] 新機能
- [ ] ドキュメント更新
- [ ] リファクタリング

## テスト
- [x] ユニットテストを追加
- [ ] E2Eテストを追加
- [x] 手動テスト完了

## チェックリスト
- [x] コードは規約に従っている
- [x] テストが通過している
- [x] ドキュメントを更新した
- [ ] CHANGELOG.mdを更新した

## スクリーンショット
（該当する場合）

## その他
追加情報
```

### 5. レビュー対応

- レビューコメントに対応
- 必要に応じて修正をコミット
- CIが通過するまで修正

---

## コーディング規約

### JavaScript

**基本スタイル**:

```javascript
// クラス名: PascalCase
class VRTextRenderer {
  constructor() {
    // 変数名: camelCase
    this.fontSize = 48;
  }

  // メソッド名: camelCase
  calculateFontSize(distance) {
    return Math.max(32, distance * 10);
  }
}

// 定数: UPPER_SNAKE_CASE
const MAX_FPS = 120;
const MIN_BUTTON_SIZE = 0.08;
```

**コメント**:

```javascript
/**
 * テキストの視覚角度を計算
 * @param {number} distance - 視距離（メートル）
 * @param {number} fontSize - フォントサイズ（ピクセル）
 * @returns {number} 視覚角度（度）
 */
function calculateVisualAngle(distance, fontSize) {
  // 実装
}

// シングルラインコメントは簡潔に
const result = calculateVisualAngle(2.0, 48); // 2mの距離で48px
```

**ES6+の使用**:

```javascript
// アロー関数
const add = (a, b) => a + b;

// 分割代入
const { fps, frameTime } = metrics;

// スプレッド演算子
const newSettings = { ...oldSettings, newValue: true };

// テンプレートリテラル
const message = `FPS: ${fps}, Frame Time: ${frameTime}ms`;

// async/await
async function loadData() {
  const data = await fetch('/api/data');
  return data.json();
}
```

### HTML

```html
<!-- インデント: 2スペース -->
<div class="vr-container">
  <button id="vr-button" class="btn-primary">
    VRモード起動
  </button>
</div>

<!-- セマンティックHTML -->
<main>
  <section class="vr-features">
    <h2>機能</h2>
  </section>
</main>
```

### CSS

```css
/* BEM命名規則 */
.vr-button {
  padding: 20px;
}

.vr-button--primary {
  background: #00ffff;
}

.vr-button__icon {
  margin-right: 10px;
}

/* インデント: 2スペース */
.vr-container {
  display: flex;
  flex-direction: column;
}
```

### ファイル命名

- VRモジュール: `vr-module-name.js`
- テスト: `module-name.test.js`
- 設定: `config-name.json`
- ドキュメント: `DOCUMENT_NAME.md`

---

## コミットメッセージ

**Conventional Commits**を使用：

### フォーマット

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type

- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメントのみの変更
- `style`: コードの意味に影響しない変更（フォーマット、セミコロン等）
- `refactor`: バグ修正や機能追加ではないコード変更
- `perf`: パフォーマンス改善
- `test`: テストの追加や修正
- `chore`: ビルドプロセスやツールの変更

### 例

```bash
# 新機能
git commit -m "feat(vr-hand-tracking): add pinch gesture detection"

# バグ修正
git commit -m "fix(vr-comfort-system): correct vignette opacity calculation"

# ドキュメント
git commit -m "docs(api): add VRTextRenderer examples"

# 複数行
git commit -m "feat(vr-environment): add cyberpunk preset

Add new cyberpunk environment preset with:
- Neon grid background
- Purple/cyan particles
- Custom lighting

Closes #45"
```

---

## テスト

### ユニットテスト

```javascript
// tests/vr-text-renderer.test.js
describe('VRTextRenderer', () => {
  test('should calculate correct font size', () => {
    const renderer = new VRTextRenderer();
    const fontSize = renderer.calculateFontSize(2.0);

    expect(fontSize).toBeGreaterThanOrEqual(32);
    expect(fontSize).toBeLessThanOrEqual(128);
  });
});
```

### テストの実行

```bash
# すべてのテスト
npm test

# ウォッチモード
npm run test:watch

# カバレッジ
npm run test:coverage
```

### カバレッジ目標

- 全体: 50%以上
- 新しいコード: 80%以上

---

## ドキュメント

### コードドキュメント

```javascript
/**
 * VRテキストレンダラークラス
 * 最適なVRテキスト表示のための研究ベースのレンダリング
 *
 * @class VRTextRenderer
 * @example
 * const renderer = new VRTextRenderer();
 * const sprite = renderer.createTextSprite('Hello VR', {
 *   fontSize: 48,
 *   distance: 2.0
 * });
 */
class VRTextRenderer {
  /**
   * 視距離に基づいて最適なフォントサイズを計算
   *
   * @param {number} distance - 視距離（メートル、デフォルト: 2.0）
   * @returns {number} フォントサイズ（ピクセル、32-128の範囲）
   * @throws {Error} 距離が負の場合
   */
  calculateFontSize(distance = 2.0) {
    if (distance < 0) {
      throw new Error('Distance must be positive');
    }
    // 実装...
  }
}
```

### APIドキュメント

新しいAPIを追加した場合は`docs/API.md`を更新：

```markdown
### newMethod(param1, param2)

説明

**Parameters**:
- `param1` (type): 説明
- `param2` (type): 説明

**Returns**: type

**Example**:
\`\`\`javascript
const result = obj.newMethod(value1, value2);
\`\`\`
```

---

## プルリクエストのレビュー

### レビュアーとして

- コードの品質、可読性を確認
- テストの妥当性を確認
- パフォーマンスへの影響を確認
- ドキュメントの更新を確認
- 建設的なフィードバックを提供

### 承認基準

- [ ] コードが規約に従っている
- [ ] テストが通過している
- [ ] カバレッジが維持・向上している
- [ ] ドキュメントが更新されている
- [ ] コミットメッセージが適切
- [ ] 破壊的変更がない（またはCHANGELOGに記載）

---

## リリースプロセス

1. **バージョン決定**（Semantic Versioning）
   - MAJOR: 互換性のない変更
   - MINOR: 後方互換性のある機能追加
   - PATCH: 後方互換性のあるバグ修正

2. **CHANGELOG更新**
   ```markdown
   ## [2.1.0] - 2025-01-15
   ### Added
   - 新機能の説明

   ### Fixed
   - バグ修正の説明

   ### Changed
   - 変更の説明
   ```

3. **バージョンタグ**
   ```bash
   git tag -a v2.1.0 -m "Release v2.1.0"
   git push origin v2.1.0
   ```

---

## コミュニティ

### コミュニケーション

- **GitHub Issues**: バグ報告、機能提案
- **GitHub Discussions**: 一般的な質問、アイデア
- **Discord**: リアルタイムチャット（準備中）

### サポート

- [Documentation](docs/)
- [API Reference](docs/API.md)
- [Usage Guide](docs/USAGE_GUIDE.md)
- [Deployment Guide](docs/DEPLOYMENT.md)

---

## ライセンス

貢献することで、あなたの貢献がMITライセンスの下でライセンスされることに同意したものとみなされます。

---

## 質問がある場合

- GitHubのIssueを開く
- Discussionsで質問する
- メールで連絡: support@qui-browser.example.com

---

**ありがとうございます！あなたの貢献がQui Browser VRをより良くします！** 🎉
