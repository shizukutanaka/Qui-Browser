{
  "name": "qui-browser-vr",
  "version": "2.0.0",
  "description": "Qui Browser VR v2.0.0 - Production-ready VR browser with 17 features (Tier 1-3), complete CI/CD automation, and enterprise monitoring. Optimized for Meta Quest 2/3 with 90-120 FPS, Japanese IME, hand tracking, spatial audio, WebGPU, multiplayer, AI recommendations, and more.",
  "main": "dist/index.html",
  "private": true,
  "homepage": "https://github.com/shizukutanaka/qui-browser#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shizukutanaka/qui-browser.git"
  },
  "bugs": {
    "url": "https://github.com/shizukutanaka/qui-browser/issues"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "proxy": "node proxy/server.js",
    "serve": "vite preview",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --collectCoverageFrom='src/**/*.js'",
    "test:tier": "jest tests/tier-system-integration.test.js",
    "test:integration": "jest --testMatch='**/tests/*integration*.test.js'",
    "icons": "node tools/generate-icons.mjs",
    "lint": "eslint src proxy",
    "lint:fix": "eslint src proxy --fix",
    "format": "prettier --write \"**/*.{js,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{js,json,md,yml,yaml}\"",
    "benchmark": "node tools/benchmark.js",
    "benchmark:all": "node tools/benchmark.js --all --iterations 500 --output benchmark-results.json --format json",
    "benchmark:ci": "node tools/benchmark.js --all --iterations 100 --output benchmark-results.json --format json",
    "benchmark:report": "node tools/benchmark.js --all --output benchmark-results.md --format markdown",
    "benchmark:regression": "node tools/check-performance-regression.js benchmark-results.json",
    "verify:docs": "node tools/verify-documentation.js",
    "verify:layout": "node tools/verify-text-layout.mjs",
    "verify:app": "node tools/verify-app-boot.mjs",
    "verify:vr-boot": "node tools/verify-vr-boot.mjs",
    "verify:prerelease": "node tools/pre-release-validation.js",
    "verify:all": "npm run verify:docs && npm run verify:prerelease",
    "ci:lint": "npm run lint && npm run format:check",
    "ci:test": "npm run test:coverage",
    "ci:benchmark": "npm run benchmark:all && npm run benchmark:regression",
    "ci:all": "npm run ci:lint && npm run ci:test && npm run ci:benchmark",
    "ci:verify": "npm run build && npm run verify:layout && npm run verify:app && npm run verify:vr-boot",
    "docker:build": "docker build -t qui-browser-vr:2.0.0 .",
    "docker:run": "docker run -d -p 8080:80 --name qui-browser qui-browser-vr:2.0.0",
    "docker:stop": "docker stop qui-browser && docker rm qui-browser",
    "docker:compose": "docker-compose up -d",
    "docker:compose:down": "docker-compose down",
    "docker:logs": "docker logs -f qui-browser",
    "deploy:gh-pages": "npm run build && gh-pages -d dist",
    "deploy:netlify": "npm run build && netlify deploy --prod --dir=dist",
    "deploy:vercel": "npm run build && vercel --prod",
    "release:patch": "npm version patch && git push && git push --tags",
    "release:minor": "npm version minor && git push && git push --tags",
    "release:major": "npm version major && git push && git push --tags",
    "clean": "rm -rf dist node_modules coverage .cache",
    "clean:install": "npm run clean && npm install",
    "gate": "npm test && npm run lint && npm run ci:verify"
  },
  "keywords": [
    "vr",
    "webxr",
    "virtual-reality",
    "meta-quest",
    "pico",
    "browser",
    "lightweight",
    "3d",
    "spatial-audio",
    "hand-tracking"
  ],
  "dependencies": {
    "three": "^0.181.0",
    "web-vitals": "^5.3.0"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "@vitejs/plugin-legacy": "^5.4.3",
    "babel-jest": "^30.2.0",
    "core-js": "^3.31.1",
    "eslint": "^9.39.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "sharp": "^0.34.5",
    "vite": "^5.4.21"
  },
  "author": "Qui Browser Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
